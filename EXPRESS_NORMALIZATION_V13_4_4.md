# V13.4.4 급행·통과역 정규화

## 적용 규칙

급행 여부를 열차번호 패턴에만 의존하지 않는다. 각 노선의 여객역 목록을 기준으로 시간표의 중간 여객역이 `arr=null`이고 `dep=통과시각`이면 해당 역은 무조건 `call=false`로 정규화한다. 여객역을 하나라도 통과하는 열차는 원본 `service`가 `local`이어도 `express`로 분류한다.

시발역은 정상적으로 `arr=null`일 수 있으므로 이 규칙에서 제외한다. 종착역도 제외한다. 여객역 목록에 없는 운전상 지점은 승하차 불가로 처리한다.

1호선 K19xx의 기존 강제 급행 규칙은 원본 데이터 이상에 대한 fallback으로 남겼지만, 승하차 가능 여부는 열차번호가 아니라 위 구조 규칙으로 결정한다.

## V13.4.3 대비 데이터 수정

- 1호선 평일: `service local→express` 62개 열차, `call true→false` 256개 행
- 1호선 휴일: `service local→express` 42개 열차, `call true→false` 112개 행
- 경의중앙선 평일: 급행 12개 열차 식별, 통과 여객역 129개 행 `call=false`
- 수인분당선 평일: 급행 22개 열차 식별, 통과 여객역 168개 행 `call=false`
- 1호선 운전상 지점 `마전`은 사용자 역 목록에서 제거하고 원본 시간표에서도 `call=false` 처리
- 기존에 정상 처리되던 경인선 급행, 경춘선 급행, 공항철도 직통은 그대로 유지

상세 변경 내역은 `TIMETABLE_NORMALIZATION_AUDIT.json`에 기록한다.

## 런타임 방어

`engine.py`의 정규화가 원본 JSON보다 우선한다. 향후 잘못된 데이터가 다시 들어와도 다음을 자동 방어한다.

1. 여객역이 아닌 운전상 지점 → 승하차 불가
2. 중간 여객역 `arr=null + dep=시간` → 승하차 불가
3. 여객역 스킵 존재 → 급행으로 분류

`server.py`는 시작 시 `timetable_integrity_report()`를 실행한다. 원본 데이터가 이 규칙과 불일치하면 `timetable_integrity_error` 이벤트를 기존 구조화 로그 시스템에 기록한다. `/api/health`에서도 `timetable_integrity` 상태를 확인할 수 있다.

## 경로 그래프

`tools/rebuild_route_graph.py`로 1호선 및 코레일계 노선만 재생성한다. 2~9호선 그래프는 이번 변경 범위가 아니므로 기존 값을 보존한다.

## 회귀 테스트

`python -m unittest discover -s tests -v`

검증 항목에는 다음이 포함된다.

- 당정 출발/도착에서 급행 승차 불가
- K1602가 월계 등 통과역에서 승차 불가
- K5702가 효창공원앞 등 통과역에서 승차 불가
- K6402가 매교 등 통과역에서 승차 불가
- 전 코레일계 노선의 구조적 통과 여객역 `call=false`
- 코레일계 `route_graph.json`이 정규화된 정차 시간표와 일치
- `마전`, `수색직결선` 같은 운전상 지점이 사용자 역 선택지에 노출되지 않음
