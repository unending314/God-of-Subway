# 지금타 변경 기록

## V14.11.7 — PWA 설치 감지/안내 강화

- `beforeinstallprompt` 이벤트가 오지 않아도 설치 버튼을 숨기지 않고 플랫폼별 설치 안내를 제공.
- Chromium에서는 실제 설치 프롬프트가 확보되면 즉시 `앱 설치` 모드로 전환.
- iOS/iPadOS는 `공유 → 홈 화면에 추가`, Android/기타 브라우저는 메뉴 기반 설치 안내 fallback 제공.
- standalone/fullscreen/minimal-ui 및 iOS `navigator.standalone`을 감지해 이미 설치된 앱에서는 설치 버튼을 숨김.
- Service Worker를 `load` 대기 없이 즉시 등록하고 `ready`/updatefound/visibility/online 상태에서 업데이트 확인.
- Service Worker 정적 캐시 키를 `jigeumta-static-v14-11-7`로 갱신. `/api/*` 비캐시 정책은 유지.


## V14.11.6 — 경의중앙선 서울역·가좌 분기 환승 보정

- 서울역 경의중앙선↔1호선: 190초. 경의중앙선→1호선은 양방향 1-1, 1호선→경의중앙선은 남영 방면 8-2 / 시청 방면 3-4.
- 서울역 경의중앙선↔4호선: 290초. 경의중앙선→오이도 방면 1-1 / 진접 방면 4-4, 4호선→경의중앙선은 숙대입구 방면 10-4 / 회현 방면 1-1.
- 서울역 경의중앙선↔공항철도: 680초. 경의중앙선 출발 4-4, 공항철도 서울역 종착 5-1.
- 서울역 경의중앙선↔GTX-A: 510초. 경의중앙선 출발 4-4, GTX-A 서울역 종착 3-2.
- 가좌 경의중앙선 분기 계통변경: 220초, 2-1. `문산방면→서울역방면` 및 `서울역발→용문방면` 방향을 구분해 적용.
- DMC의 동일노선 역방향 90초/60초 특례와 행신~DMC 3초 추정 규칙은 적용하지 않음. 실제 운행계통과 단순한 가좌 분기 규칙을 우선해 오경로/과도한 예외 처리를 방지.
- 서울역발 열차가 가좌 이후에도 실제로 계속 운행 가능한 경우 가좌에서 강제 환승시키지 않음.

## V14.11.5 — 직전열차 실시간 포착 + 경의선 서울역 계통 + PWA
- 열차 후보군의 `직전` 열차가 realtimePosition에 실제로 잡혀 있어도 항상 `미포착`으로 표시되던 버그 수정. exact live/live_context 관측이 있으면 직전 후보도 `실시간 확인`으로 승격하고 현재역/상태/관측시각을 그대로 노출.
- `경의선 전동열차 시각표(2026.4.20.부터).xlsx` 반영: 평일 62편, 토·휴일 44편 추가. 서울역–신촌–가좌 지상 경의선 분기를 기존 경의중앙선 시간표에 병합.
- 지상 신촌역은 2호선 `신촌`과 충돌하지 않도록 `신촌역(경의선)`으로 독립 역명 처리. 경의중앙선 realtimePosition의 `신촌`만 line-aware 정규화로 연결.
- 평일 K250x 급행 16편의 통과역을 DIA 구조에서 판별해 급행/비정차로 반영.
- PWA 추가: manifest, 192/512 아이콘, Service Worker, 설치 버튼, 새 버전 적용 버튼. `/api/*`는 Service Worker 캐시에서 완전 제외하여 실시간/경로 응답이 오래된 캐시로 대체되지 않도록 함.
- route_graph를 새 시간표에서 재생성하고 회귀 테스트 추가.

# V14.11.4 — 2026-08-22

## 디지털미디어시티 환승시간 보정
- 6호선 ↔ 경의중앙선: 2분 20초(140초)
- 공항철도 ↔ 6호선: 6분 30초(390초)
- 공항철도 ↔ 경의중앙선: 9분 20초(560초)
- 양방향 directed pair의 default/방향별 record seconds를 동일하게 갱신.

# V14.11.3 — 2026-08-22

## 4호선 개정 시각표
- 사용자 제공 `안산과천선 전동열차 시각표(2026.8.22.부터).xlsx` 반영.
- 평일 475편 / 휴일 398편으로 교체.
- K440x 평일 급행의 통과역을 DIA의 `도착 없음 + 통과시각` 구조로 판별하여 승차 후보에서 제외.

## 수인분당선 개정 시각표
- 사용자 제공 `수인분당선 전동열차 시각표(2026.8.22.부터).xlsx` 반영.
- 평일 437편 / 휴일 342편.
- K63xx/K64xx 등 개정 열차번호와 평일 급행 22편의 통과역 구조 반영.
- 기존 DIA 연계열번은 동일 열차번호가 유지되고 연계 대상도 새 시각표에 존재하는 경우에만 보존.

## 검증
- 원본 XLSX의 시발역/종착역과 변환 후 첫/마지막 역 전수 대조.
- 각 열차 시각 단조 증가 및 자정 이후 익일시각 변환 검증.

# V14.11.2 — 2026-08-22

## Fixes
- 코레일 계열 시발역의 조기 `진입/도착` API 행을 출발/지연 관측에서 제외. 다음 역 관측부터 실시간 반영.
- 기존 Redis exact-delay 캐시에 남은 시발역 지연 관측도 읽기 단계에서 무시해 -20분대 조발 오염을 즉시 차단.
- 자동 경로 후보 생성에 환승 가산점 기반 topology diversity seed를 추가. 정적 최소 주행시간 후보군에 가려진 저환승·고빈도 경로도 실제 ETA 평가 대상으로 포함.
- 운정→동탄 회귀 테스트 추가: 2026-08-22 14:41 기준 기존 17:20/5환승 경로 대신 17:04 도착 경로 선택.
- K5083 회귀 테스트 추가: 문산 시발역 관측 무시, 파주 첫 유효 관측부터 정시 지연 반영.

## Validation
- `pytest`: 85 passed.
- 성균관대→마포 schedule-only warm: 약 0.20초.
- 운정→동탄 schedule-only warm: 약 0.26~0.27초.

# V14.11.1 — 2026-08-22

- Vercel `/api/:path*`를 Railway persistent FastAPI로 rewrite하도록 `vercel.json` 변경.
- 웹 프론트엔드의 기존 `/api/*` 호출 계약은 유지.
- 런타임 버전을 `V14.11.1`로 갱신.
- Railway API origin: `https://jigeumta-api-production.up.railway.app`.

# V14.11.0 — 2026-08-22

- `/api/auto_route`, `/api/route`, `/api/trip_update` 성공 응답에 실시간 source 진단정보를 추가했습니다.
- `diagnostics.realtime_source`로 `redis` / `direct` / `mixed` / `schedule` 사용 여부를 확인할 수 있습니다.
- `diagnostics.cache_state`, `cache_age_seconds`, `source_age_seconds` 및 노선별 상세 상태를 함께 제공합니다.
- 진단정보는 UI에 표시하지 않으며 Redis URL, 비밀번호 등 비밀정보를 노출하지 않습니다.

# V14.10.0 — 2026-08-21

- 중앙 realtime collector 기본 주기를 5초로 전환하고 최대 24개 병렬 수집을 지원.
- 5초 polling은 유지하되 서울시 원천 snapshot이 동일하면 health만 갱신하고 snapshot/train-state/delay Redis write는 생략.
- Redis에 raw API train state를 추가하여 successful snapshot에서 사라진 열차를 즉시 삭제하지 않고 `missing_recent`으로 보존.
- 2호선 본선 API 열번 3xxx~8xxx를 2xxx로 정규화하고 raw trainNo는 별도 보존.
- 2호선 9xxx 임시/특발, 99xx 시운전 후보를 정규 시간표와 분리.
- 2호선 대규모 조발을 허용해 30분 이상 시간표 차이만으로 context 재매칭하지 않도록 수정.
- 2호선도 Redis exact-delay fallback 대상에 포함하고 큰 조발 지연값을 보존.
- realtime status에 cache/source age 및 live/missing/special train-state 통계를 추가.

# V14.9.8 — 2026-08-21

- 2호선 순환선에서 추천 방향과 반대 방향의 `직전` 열차가 표시되던 오류 수정
- 2호선 사용자 후보군을 선택된 내선/외선 방향으로 제한
- `근처 열차 후보` → `다른 열차 후보`, `앞 열차를 탄 것 같아요` → `다른 열차를 탄 것 같아요`
- 다른 열차 버튼은 즉시 탑승 처리하지 않고 후보 목록을 펼치도록 변경
- 후보 요약의 `미포착 열차 포함` 문구 제거, 개별 후보 `미포착` 배지는 유지
- 후보 출발/도착 시각 시인성 강화
- 2호선 실시간 열번 직접 매칭 실패 시 내/외선·현재역·관측시각 기반 보수적 문맥 매칭 추가

# V14.9.7 — 2026-08-21

- 대안 경로 목록 기본 접힘 및 `경로 직접 선택` 가시성 강화
- 자동경로 요약의 중복 ETA/후보 비교 문구 제거
- 현재 운행상황 요약에서 중복 도착시각/설명문 제거, 총 소요시간·도착예정 강조
- 모바일 `지금 기준으로 다시 찾기` 로딩 중 버튼 크기 고정
- 모바일 열차 추적 `현재 위치` 영역을 전폭 배치해 긴 역명 줄바꿈 개선

## V14.9.6 — 빠른환승 위치 완성 및 실험 UI 정리

- 사용자 제공값으로 빠른환승 실제 잔여 6개 방향 조합을 모두 보강했습니다.
- 상세 설정에서 `기존 앱 예상 총시간`, `기말 실험 자동 기록`, `실험 도구 화면 표시` UI를 제거하고 자동 실험 기록을 기본 비활성화했습니다.

## 빠른환승 방향 정규화
- 빠른환승 위치 매칭을 `첫 시간표 열차의 다음 정차역` 하나에 의존하던 방식에서, 실제 시간표의 운행 순서를 이용해 환승 방향키를 정규화하는 방식으로 변경했습니다.
- 급행이 인접역을 통과하는 경우에도 물리적 방향의 기존 환승 위치를 찾을 수 있습니다.
- GTX-A 서울역/수서, 김포골드라인 김포공항, 신림선 샛강처럼 환승역이 종착역인 경우에는 마지막 접근 방향키를 사용합니다.
- 6호선 응암순환처럼 일방통행 구간에서 데이터가 접근역 기준으로 기록된 경우도 안전하게 매칭합니다.
- `대모산입구`와 시간표 내부 `대모산` 표기를 동일 역으로 정규화했습니다.
- 국토교통부 원본을 다시 대조해 인천시청 인천2호선→인천1호선의 4개 방향 조합을 올바르게 복원했습니다.
- 합정→연신내→운정중앙 예시에서 연신내 6호선→GTX-A 빠른환승 `6-2`가 정상 표시됩니다.
- 현재 시간표 기준 가능한 환승 방향 조합을 재감사한 결과, 빠른환승 위치가 실제로 남아 있지 않은 조합은 6개입니다.
- 전체 회귀 테스트 65개 통과.

# V14.9.4 — 2026-08-20

## 현재시각 기준 경로 재검색
- 이동 경로 헤더의 계산 기준시각 옆에 `지금 기준으로 다시 찾기` 버튼을 추가했습니다.
- 버튼은 일반 경로 결과가 계산된 뒤에만 표시되며, 현재 표시 중인 출발역/도착역으로 현재시각 기준 자동경로 후보 비교를 다시 수행합니다.
- 상단 입력부까지 스크롤하지 않아도 동일한 이동의 최신 최적경로를 다시 탐색합니다.
- `이 열차를 탔어요`로 추적을 시작하는 즉시 버튼을 숨기고, 추적 중에는 기존 `↻ 새로고침`만 유지합니다.
- 세션에서 추적 상태를 복원한 경우에도 재검색 버튼을 노출하지 않습니다.


## V14.9.3 — 모바일 시간 선택 / 검색 아이콘 렌더링 수정
- 모바일 직접 시간 선택을 OS 원형 시계 피커 대신 앱 내부 3열(오전·오후 / 시 / 분) 피커로 교체.
- 경로 조회 후 버튼에 남던 SVG 기본 fill 삼각형을 제거하고 `경로 조회` 텍스트 상태로 복원.
- 역 자동완성 노선 아이콘을 inline SVG 대신 브라우저 호환성이 높은 HTML/CSS 배지로 렌더링.
# V14.9.3 — 2026-08-20

- `God-of-Subway-dev-ts-bun`의 초기 검색 입력 UI만 이식했습니다.
- 출발/도착역 카드, 원형 역 전환, 직접 시간 선택, ±1/±5분, 경로 조회 UI를 동일 계열 디자인으로 정리했습니다.
- V14.8의 결과 화면, 대안경로 선택, 1호선 추월 보정 로직은 유지합니다.
- 상세 설정은 기능 보존을 위해 검색 카드 하단 보조 버튼으로 유지합니다.

# V14.8.0 — 2026-08-20

- 1호선 공식 시간표에서 완행 대피정차 중 급행이 통과해 운행순서가 뒤집히는 계획 추월 관계를 자동 검출.
  - 평일 59건, 휴일 48건.
  - 평일 추월역: 군포 38, 오산 8, 도봉산 4, 수원 4, 병점 3, 평택 2.
  - 휴일 추월역: 군포 31, 오산 8, 수원 6, 병점 3.
- 실시간/최근 열차별 지연을 단순 독립 가산하지 않고, 계획 추월역 이후에는 완행이 급행보다 먼저 나가지 않도록 대피 대기시간을 ETA에 추가.
- K693(+1분) / K1951(+5분) 예시에서 신도림→성균관대 추천이 K693으로 뒤집히던 문제 수정: K1951 21:15, K693 21:16으로 계산.
- 화면에 `급행 추월 예정` / `급행 추월 대기` 운행정보를 열차 후보와 선택된 구간에 표시. 현재 실시간 지연(+N분)은 그대로 두고 미래 운전정리 대기분을 별도 표시.
- 급행 지연이 너무 커 추가 대피가 10분을 넘는 경우에는 강제 순서보정 대신 `운전정리 변동 가능`으로 표시.
- 추월 관계는 `route_graph.json`에 사전 계산해 저장하여 런타임 전수검색 없이 즉시 조회.
- 추월 이전 목적지에는 대피 보정을 적용하지 않는 회귀 테스트 추가. 전체 55 tests passed.

# V14.7.0 — 2026-08-20

- 자동경로 응답의 추천안/대안안에 사용자 선택 버튼 추가. 선택 시 해당 segments를 즉시 로드하고 ETA를 재계산.
- 우측 상단 버전 표기를 `/api/health`의 서버 버전과 단일화하여 하드코딩 불일치 제거.
- 4호선↔수인분당선, 서해선↔경의중앙선 공유선로에서 동일 진행방향 환승 보행시간을 0초로 보정. 열차 대기시간은 기존 시간표 계산에서 별도로 유지.
- 금정 1호선↔4호선 64초 짧은 환승시간 유지 및 회귀 테스트 추가.

# V14.6.0 — 2026-08-20

- 총신대입구(이수) 4호선↔7호선 환승시간을 사용자 제공 8개 방향 조합으로 반영.
- 4호선→7호선: 동작→남성 190초, 동작→내방 230초, 사당→남성 190초, 사당→내방 230초.
- 7호선→4호선: 남성→동작 230초, 남성→사당 230초, 내방→사당 190초, 내방→동작 190초.
- 자동 경로 탐색의 방향 미확정 단계에서는 보수적으로 230초를 대표 환승비용으로 사용하며, 실제 경로 확정 후 정확한 방향별 시간으로 재계산.
- route_graph 재생성 및 방향별 환승 회귀 테스트 추가.

# V14.5.0 — 2026-08-20

- 사용자 완성 환승 CSV 반영.
  - 빠른환승 위치 입력 대상 132개 방향 조합을 모두 반영.
  - 환승시간 입력 대상 103개 방향을 모두 반영.
  - 신규 34개 환승 관계는 양방향 모두 빠른환승 위치가 존재하도록 완료.
  - `인천시청 인천1호선 → 인천2호선`은 사용자 메모에 따라 기본 260초, 인천1호선 하행(예술회관 방향)에서만 130초 적용.
- 기존 국토부 공개데이터의 방향 엄격 매칭 정책 유지.
- `transfer_data.json` 메타데이터에서 삭제된 과거 master/version 흔적을 정리하고 런타임 JSON을 minify.
- 회귀 테스트 기대값을 새 환승시간에 맞게 갱신.

# V14.4.0 — 2026-08-20

- 국토교통부 `철도역 빠른 환승 정보_20250923` 반영.
  - 기존 위치 데이터는 보존하고, 비어 있던 환승 관계에 한해서 방향이 확정되는 차량-문 위치만 추가.
  - 추가 8개 노선 관련 13개 환승 관계에서 최소 한 방향 이상 보강.
  - 신림선/GTX-A/김포골드라인은 해당 공개데이터에 없어 이번 패치 대상에서 제외.
  - 공개데이터에 환승시간 정보가 없어 신규 관계는 180초 fallback 유지.
- `direction_strict` 레코드 지원. 공개데이터에 명시된 방향 조합이 아니면 빠른환승 위치를 다른 방향에 추정 적용하지 않음.
- 기존 V14.3 clean 프로젝트 구조 유지.

# V14.3.0 — 2026-08-20

- 인천2호선 서해구청 실제 역 시각표 반영.
  - 평일 465개 운행편, 주말/휴일 369개 운행편 모두 기존 시간표 운행편과 매칭.
  - 기존 보간값 833개를 실제 도착/출발 시각으로 교체.
  - 서해구청 관련 `estimated=true` 제거.
  - 인천2호선 전체에서 남은 보간 stop은 95개이며 서해구청 외 원본 공란에 한정.
- 프로젝트 clean-up.
  - 중복 `base/` 제거.
  - V13/V14 과거 버전별 문서 및 audit 산출물 제거.
  - 인천1/2호선 import 원본/중간 CSV/XLSX/JSON 제거.
  - 실행에 사용하지 않는 transfer master 원본과 변환 도구 제거.
  - 런타임 JSON minify 적용.
- 기존 realtime foundation, 8개 추가노선, GTX-A 단절 구조 유지.

## V14.11.0 — automatic-route latency reduction
- Added O(1) canonical station→line and line→station lookup indexes for route-search hot paths.
- Added immutable stop-list station/route-pair caches to avoid repeated full timetable scans.
- Added OD/service-mode topology cache (LRU 2048) so repeated route searches reuse Yen K-shortest topology results.
- Removed the redundant schedule-only scoring pass from automatic routing. Each candidate is now scored once against the shared realtime cache; calculate_route keeps the existing timetable fallback behavior.
- Added `diagnostics.performance_ms` with candidate generation, realtime prefetch, candidate scoring, and total engine latency.
- Added Railway persistent API deployment package for future web/mobile shared backend.
