# V13.4.9.0
- 신분당선 생산 ETA를 realtimePosition 열번 매칭에서 realtimeStationArrival + Rail.Blue DIA 구간소요시간 방식으로 변경.
- 신분당선 realtimePosition.trainNo는 진단용으로만 사용하며 신뢰도 승격 근거에서 제외.
- `/api/debug/sinbundang_probe` 및 `tools/probe_sinbundang_api.py` 추가.

# V13.4.8 — 신분당선 실시간 매칭 보강

- realtimePosition 신분당선 요청값을 사용자 확인값 `1077:신분당선` 우선으로 변경
- `1077:신분당선`이 0건/오류일 경우 `신분당선` 단독 요청으로 자동 fallback
- API 열차번호가 Rail.Blue `DX####` 운행열번과 다른 체계여도 현재역·방향·종착역·관측시각으로 DIA 보조 매칭
- 열차번호 exact 매칭은 `높음`, 문맥 기반 보조 매칭 및 live median은 `중간`, 실시간 근거가 없을 때만 `낮음`
- 신분당선 운영사 병기 역명(광교(경기대), 광교중앙(아주대) 등) 정규화 추가
- 실시간 행 수신 후 시간표 매칭 0건이면 `realtime_train_match_failure` 구조화 로그 자동 기록
- `/api/health`에 `realtime_query_aliases` 노출

# V13.4.7 — 신분당선 realtimePosition 최초 연동

- 신분당선 서울시 실시간 열차 위치 API 지원 (`subwayId=1077`)
- 최초 구현은 `subwayNm=신분당선` 요청과 DX 열번 숫자부 fallback에 의존
- exact/live median/schedule-only 신뢰도 승강 및 최근 지연 캐시 지원

# V13.4.6 — 신분당선 시간표 추가

- 사용자 제공 `신분당선 시간표.xlsx`의 4개 시트(평일 상/하, 주말 상/하)를 `sinbundang_schedule.json`으로 정규화
- Rail.Blue 좌우 화면 복사 과정의 중복 열차 열 제거: 평일 326편, 주말 272편
- 운전상 지점 `광교기지`, `분당연결선분기`, `판교주박기지`를 non-callable로 유지
- 주말 시각표를 토요일(SAT)·일요일/공휴일(END)에 동일 적용
- 신분당선을 schedule-only 노선으로 추가하여 realtimePosition API를 호출하지 않고 공식 시간표 ETA 사용
- 신분당선 역 16개를 UI 및 자동 경로 탐색에 추가하고 route_graph 재생성
- 신분당선 단독/환승/주말/실시간 API 미호출 회귀 테스트 추가; 전체 21개 테스트 통과

# V13.4.5 — 직전 열차 탐색 누락 수정

- 직전 열차 탐색의 고정 10분 제한 제거: 경의중앙선 등 장배차 노선에서도 실제 바로 앞 열차를 반환
- 짧은 구간에서 직전 열차가 이미 목적지에 도착했다는 이유로 숨기던 `도착 후 2분` 필터 제거
- 직전 열차는 `ready_dt` 직전 실제 출발시각을 기준으로 전 노선 공통 계산
- 경의중앙선 장배차/단거리 누락 회귀 테스트 추가

# V13.4.4 — 전 코레일계 급행/통과역 구조 기반 정규화

- 열차번호 하드코딩 대신 `중간 여객역 arr=null + dep=통과시각` 구조로 통과역 판정
- 경의중앙선 평일 급행 12개 열차(K570x/K585x 계열) 및 통과역 129개 행 보정
- 수인분당선 평일 급행 22개 열차(K640x/K680x 계열) 및 통과역 168개 행 보정
- 1호선 K19xx 외 K160x 등 다른 급행 계통까지 동일 규칙 적용
- 운전상 지점 `마전`을 1호선 사용자 역 목록에서 제거하고 승하차 불가 처리
- 코레일계 route_graph를 정규화된 정차역 기준으로 재생성; 2~9호선 그래프는 보존
- 서버 시작 시 시간표 무결성 검사 추가; 이상 시 `timetable_integrity_error` 구조화 로그 기록
- `/api/health`에 `timetable_integrity`, `route_graph_version` 추가
- 전 코레일계 통과역/급행/경로 그래프 회귀 테스트 확대(12개 테스트)

# V13.4.3 — 급행 통과역 판정 수정 + 오류 관측 시스템

- 1호선 K19xx 급행의 `arr=null` 통과역이 `call=true`로 남아 승하차 가능으로 오판되던 중대 오류 수정
- 평일 726건 / 휴일 672건의 K19xx 통과역 `call=false` 데이터 교정
- 엔진 정규화 단계에도 급행 통과역 자동 보정 방어 로직 추가
- 잘못된 통과역 정차 플래그를 반영해 생성됐던 1호선 `route_graph.json` 가중치도 정상 정차역 기준으로 재생성
- 당정 출발/도착 시 급행이 추천되지 않는 회귀 테스트 추가
- 서버/엔진/브라우저 오류에 UUID `error_id`, `request_id` 부여
- Vercel stdout 구조화 JSON 로그 + 로컬 JSONL 로그 추가
- `DATABASE_URL` 설정 시 PostgreSQL `app_error_logs` 영구 저장 지원
- 필요 시 `LOG_LOW_CONFIDENCE=1`로 `low_confidence_route` warning 로그 수집 가능
- 로컬 `app.py`가 별도 구버전 ETA 엔진을 복제하지 않고 `engine.py`를 직접 사용하도록 통합

# 변경 기록

지금타의 버전별 변경 내역입니다.

> **참고**
> V10 이후 항목의 버전 번호가 과거 일괄 치환으로 전부 `V13.2.2`로 덮어써졌습니다.
> 원래 번호는 복원할 수 없어 기록된 그대로 두었습니다.
> 실제 순서는 아래에 나열된 순서(위가 오래된 것)를 따릅니다.
> V13.3.0부터는 GitHub Releases에서 태그와 함께 관리합니다.

---

V7 — 이동 중 라이브 열차 추적
- 경로 계산 결과의 각 구간에 '이 열차 탑승' 버튼 추가
- 버튼을 누르면 해당 열차번호를 현재 구간에 잠금
- 20초마다 realtimePosition으로 그 열차만 계속 추적
- 탑승 열차의 최신 환승역 ETA를 seed로 이후 모든 구간을 다시 계산
- 다음 구간이 처음에는 시간표+현재 지연 추정(중간 신뢰도)이더라도,
  실제 후보열차가 realtimePosition에 나타나면 자동으로 높은 신뢰도로 전환
- 다음 추천열차가 바뀌거나 신뢰도가 높음으로 전환되면 화면 알림
- 환승 후 다음 열차의 '이 열차 탑승'을 누르면 추적 대상을 새 열차로 전환
- 현재 라이브 추적 상태는 sessionStorage에 저장되어 같은 탭 세션의 새로고침에도 복원
- '추적 종료' 버튼 제공

API
POST /api/trip_update
  active_index
  boarded_train_no
  boarded_at
  segments
  day

주의
- 실시간 위치 API가 특정 열차를 잠시 누락해도 열차 잠금은 유지합니다.
  이때는 공식 시간표 + 현재 동일 방향/등급 지연값으로 임시 계산하며 신뢰도 '중간'으로 표시합니다.


V8 — 시발역/출발 직후 미포착 열차 대응
- 각 구간에서 실시간 후보 + 공식 시간표 후보를 동시에 구성
- 승차 준비시각 기준 약 60분 이내 열차를 최대 6개 '근처 열차 후보'로 표시
- 후보 표시:
  · 실시간 확인
  · 시간표 후보 · 미포착
- 동일 열차번호가 realtimePosition에 등장하면 실시간 후보가 자동 우선
- 미포착 후보에서도 실제 탑승 후 '탑승' 버튼 사용 가능
- 탑승 시 열차번호 고정 후 V7의 20초 라이브 추적 시작
- realtimePosition에 이후 포착되면 신뢰도 중간 → 높음 자동 전환

핵심 원칙
'실시간에 안 보인다 = 열차가 없다'가 아니다.
공식 시간표를 열차 존재 후보로 사용하고, realtimePosition을 실시간 운행 확인 신호로 사용한다.


V9 — 경의중앙선 / 수인분당선 추가

추가 시간표
- 경의중앙선: 사용자 제공 공식 시간표
  · 평일 183개 열차
  · 휴일 149개 열차
  · 53개 역
- 수인분당선: 사용자 제공 공식 시간표
  · 평일 437개 열차
  · 휴일 342개 열차
  · 63개 역

지원 방식
- 서울시 realtimePosition API:
  · 경의중앙선 subwayId 1063
  · 수인분당선 subwayId 1075
- 공식 시간표의 trainNo와 실시간 trainNo를 매칭
- 현재역 예정시각과 실제 수신시각으로 지연 계산
- 미래 역 ETA, 다중 환승, 시간표 미포착 후보, 라이브 탑승 추적 모두 기존 노선과 동일하게 지원

DIA 표기 보정
- 디엠시 → 디지털미디어시티
- 홍대입 → 홍대입구
- 효창공 → 효창공원앞
- 항공대 → 한국항공대
- 로데오 → 압구정로데오
- 강남구 → 강남구청
- 남동인 → 남동인더스파크
- 수원시 → 수원시청
- 신인천 → 인천
- 신수원 → 수원
- 신길온/신길온천 → 능길
- 기타 약칭도 앱 내부 canon_station에서 통일

연계열번
- 엑셀 마지막 '연계열번' 행을 보조 trainNo alias로 저장
- 단, 실제 열차번호가 존재하면 실제 번호가 항상 우선


V9.1 — 탑승시각 UTC/KST 오류 수정

원인
- 프론트엔드에서 탑승시각을 new Date().toISOString()으로 저장
- toISOString()은 UTC이므로 한국 로컬시각보다 9시간 느린 값이 백엔드에 전달됨
- 도착 ETA는 로컬시각으로 계산되어 '차내 이동시간'만 약 9시간 크게 표시됨

수정
- 탑승시각을 브라우저의 로컬 년/월/일/시/분/초로 직접 직렬화
- 기존 sessionStorage의 잘못된 boardedAt을 복원하지 않도록 TRIP_KEY 버전 갱신
- 도착 ETA 계산 로직과 시간표 데이터는 변경하지 않음

예
- 실제 탑승 15:00
- 기존 저장값 06:00 (UTC) -> 잘못된 차내시간 약 +9시간
- 수정 저장값 15:00 (KST/local) -> 정상


V13.2.2 — 대한민국 공휴일/대체공휴일 자동 운행일 + 급행 표시 강화

AUTO 운행일
- 일반 평일 -> DAY
- 일반 토요일 -> SAT
- 일요일 -> END
- 법정 공휴일 -> END
- 대체공휴일 -> END
- 2026~2035 공휴일 달력을 앱에 번들하여 런타임 외부 API 없이 판정
- 설날/추석/부처님오신날의 음력 날짜도 포함
- 현행 2026년 규정에 따라 노동절·제헌절도 공휴일에 포함
- 대체공휴일 규칙을 반영
- 임시공휴일/선거일처럼 정부가 수시 지정하는 날은 자동 달력에 없을 수 있으므로 수동 END 선택 가능

UI
- AUTO 선택 시 '광복절 → 휴일 시간표', '광복절 대체공휴일 → 휴일 시간표'처럼 판정 근거 표시
- 급행열차는 열차번호 옆에 눈에 띄는 '급행' 배지 표시
- 시발역 출발대기/조발 관련 기존 지연 표시는 변경하지 않음


Vercel 배포판
- 직접 ThreadingHTTPServer를 실행하지 않음
- server.py의 FastAPI app을 Vercel Python Function으로 실행
- 계산 코드는 engine.py
- SEOUL_API_KEY는 Vercel Environment Variable로만 주입


## V13.2.2 Vercel KST 수정
Vercel 런타임은 기본 UTC이므로 Python의 `datetime.now()`를 직접 사용하면
서울시 시간표/실시간 API(KST)와 9시간 차이가 발생합니다.

V13.2.2부터 모든 운행 계산의 현재시각을 `Asia/Seoul`로 고정합니다.
- 실시간 열차 후보 판정
- API 데이터 freshness
- AUTO 공휴일 판정
- 라이브 추적
- 승차 가능시간 계산

프론트엔드의 사용자 입력시각도 한국시간 기준으로 동일한 시간축에서 비교됩니다.


## V13.2.2 UI 개선
- 열차 후보/선택 열차에 실제 운행 시발역 → 종착역 표시
- 일반열차 / 급행열차 구분을 텍스트로 명확히 표시
- 급행 배지는 기존처럼 유지
- 역 입력창 클릭만으로 전체 역 목록을 표시하지 않음
- 한 글자 이상 입력했을 때만 검색 후보 표시
- 초성 검색 지원
  · `ㅅ` → 초성이 ㅅ으로 시작하는 역
  · `ㅅㄱ` → 초성이 ㅅㄱ으로 시작하는 역
  · `성` → '성'으로 시작하는 역
- 최대 8개 후보 표시, ↑/↓/Enter/Esc 키보드 조작 지원


## V13.2.2 — 자동 지하철 길찾기
- 출발역/도착역만 입력하면 지원 노선 전체에서 빠른 경로 자동 탐색
- 공식 열차별 시간표에서 생성한 station-line 그래프를 Dijkstra로 탐색
- 비용 = 시간표 기반 차내 주행시간 + 환승 기본 4분
- 자동 생성 경로를 기존 구간 편집기에 바로 채움
- 이후 기존 realtimePosition ETA 엔진으로 즉시 재계산
- 자동 경로 생성 후에도 노선/역/환승시간을 사용자가 직접 수정 가능
- 초성 검색 지원
- 현재 지원: 1~9호선, 경의중앙선, 수인분당선

예:
마포구청 → 성균관대
6호선 마포구청→합정
2호선 합정→신도림
1호선 신도림→성균관대

주의:
- V13.2.2 자동 경로 탐색 자체는 번들된 공식 시간표 그래프 기준
- 이후 실제 도착시간은 실시간 열차 위치/지연으로 별도 재계산
- 같은 이름이지만 서로 다른 역인 5호선 양평 / 경의중앙선 양평은 환승 연결에서 제외


V13.2.2 — 기말 실험 데이터 수집
- ETA 계산 성공 시 실험 세션 자동 생성
- 최초 ETA / 최신 ETA / ETA 갱신 이력 저장
- 실제 탑승 열차 및 추천 열차 일치 여부 저장
- 목적지 실제 도착시각 버튼
- 기존 앱 예상 총시간과 비교해 ETA MAE 계산
- 첫 승강장 대기시간 계산
- 완료 실험 KPI 요약
- CSV 요약 내보내기
- 상세 JSON 내보내기
- 데이터는 브라우저 localStorage에 저장

V13.2.2 추가:
- 생성 로고를 헤더/favicon/apple-touch-icon에 적용
- 1호선 K19xx 전부 급행 판정
- 후보 열차 실시간 소재 표기
- 실시간 미포착 급행/후보 예상 소재 표기


V13.2.2 — Google Analytics
- Google Analytics 4 Measurement ID: G-HHYSX8GQ95
- gtag.js를 index.html <head>에 삽입
- 기본 page_view 측정 활성화
- 별도 Vercel 환경변수는 필요 없음


V13.2.2 — 검색 중심 대규모 UI/환승 업데이트
- 초기 화면은 출발역/도착역 + 조회 + 상세설정 톱니바퀴만 표시
- 기본 플랫폼 도착은 +10분, 상세설정에서 변경
- 수동 노선 구간 편집 UI 제거(내부 자동 경로 데이터만 유지)
- 02:00 이전 AUTO 운행일은 전날 기준
- 서울교통공사 환승 데이터로 역/노선별 실제 환승시간 적용
- 방향별 빠른 환승 하차 호차-문 / 다음 열차 승차 호차-문 표시
- 구간 결과: 승하차 예정시각, 차내시간, 열차번호, 소재, 지연, 승차역 도착예정 표시
- 버튼 문구 '이 열차를 탔어요'로 변경
- 기말 실험 도구는 상세설정에서 선택한 경우에만 노출
- Google Analytics G-HHYSX8GQ95 유지


V13.2.2 — 환승 표기 및 결과 새로고침
- 빠른 환승 표기는 현재 열차에서 내릴 위치만 표시: `빠른 환승 · 5-1`
- 다음 열차 승차 위치 표기는 제거
- 환승거리는 기존처럼 병기
- 이동 경로 상단에 계산 기준시각 + 고정 플랫폼 도착시간 표시
- 새로고침 버튼 추가
- 새로고침은 경로/플랫폼 도착시간을 바꾸지 않고 실시간 열차 위치·지연·ETA만 재조회
- 플랫폼 도착시각이 이미 지난 뒤 새로고침할 경우 과거 열차가 다시 추천되지 않도록 현재시각 이후의 탑승 가능 열차부터 재평가
- 탑승 열차 라이브 추적 중 새로고침 버튼은 즉시 live trip poll 수행


V13.2.2 — 새로고침 파서 / 2·6호선 연속운행
- 새로고침 시 full datetime(YYYY-MM-DD HH:MM:SS)을 HH:MM 파서에 넣던 오류 수정
- 2호선: 성수에서 같은 방향의 후속 열번이 짧은 시간 내 이어지는 경우 동일 물리열차로 연결
- 6호선: 응암 종착 UP 열번과 응암 시발 DOWN 열번이 짧은 시간 내 이어지는 경우 동일 물리열차로 연결
- 연결 조건: 특수 경계역 + 노선별 방향 조건 + 최대 180초 + 가장 가까운 유일 후보 + 후속열번 1:1 사용
- 6호선 토요일 6044 → 6055 연속운행 확인
- 2호선 토요일 2006 → 2036 연속운행 확인
- 자동 길찾기에서 같은 차량의 열번 변경을 환승으로 취급하지 않음
- 실시간 후보/시간표 후보도 열번 변경 전후를 하나의 탑승 가능한 구간으로 계산
- '이 열차를 탔어요' 추적 중 열번이 바뀌면 후속 열번으로 잠금을 자동 승계
- 2호선 후속 열번을 실시간 추적할 때 공식 시발/종착(예: 성수 → 성수)을 그대로 표시하고 임의 '내선순환/외선순환' 명칭을 생성하지 않음


V13.2.2 — 후보열차 소재 상태코드 수정
- `실시간 소재 · 신당 3`처럼 원시 숫자 상태코드가 표시되는 문제 수정
- 알려진 상태 0/1/2만 `진입/도착/출발`로 표시
- 3 등 미정의 숫자 상태는 화면에서 숨김
- 프론트엔드에서도 끝에 붙은 숫자 상태코드를 방어적으로 제거


V13.2.2 — 2호선 연속열번 판정 개선
- 6호선: 수동 검증 결과 기존 최대 180초 판정을 유지
- 2호선: 성수에서 180초(3분) 간격 연결은 전부 배제
- 2호선: 종착열번 순차 greedy 대신 전체 후보 중 가장 짧은 연결부터 1:1 매칭
- 평일 2089(09:04:30 성수 종착) → 2151(09:05:00 성수 시발)을 30초 후속열번으로 정상 판정
- 2087(09:02:00 성수 종착) → 2151의 잘못된 180초 연결 제거


V13.2.2 — 모바일 UI 및 플랫폼 도착 기본값 개선
- 상세설정의 기본값/AUTO 설명문 삭제
- 플랫폼 도착 기본값을 +10분에서 `지금`으로 변경
- `-5분` 빠른 선택 추가
- 모바일 후보열차 정보를 압축: 긴 출도착/열차종류/지연/출처를 한 줄 중심으로 표시
- 모바일에서는 `시간표 후보 · 미포착` → `미포착`, `급행열차` → `급행`으로 축약
- 소재 정보는 다음 한 줄에 유지
- 후보 선택 버튼은 모바일에서 전체 너비로 정리
- 모바일 조회 버튼을 톱니바퀴 옆의 넓은 primary 버튼으로 변경


V13.2.2 — 여정 상태 추적 / 시간표 fallback / 빠른 시간 조절
- realtimePosition API 장애 시 경로 전체 실패 대신 공식 시간표 기반 결과 제공
- 추적 중 realtimePosition 장애 시에도 시간표 기반 예상 소재/ETA 유지
- 목적역 도착 또는 출발 상태를 감지하면 해당 구간을 즉시 완료 처리
- 현재 탑승 구간 glow / 완료 구간 grayscale 처리
- 환승 단계 glow + `환승을 마쳤어요` 수동 버튼
- 환승시간이 끝나면 다음 구간이 자동 활성화
- `추적 종료` → `이제 내렸어요`
- 탑승 전 새로고침 숨김
- 탑승 중 새로고침은 현재 열차 최신 하차 ETA + 환승시간으로 후속열차 재계산
- 환승 중 새로고침은 실제 하차시각 + 환승시간 기준
- 환승 완료 후 새로고침은 현재시각 기준
- 첫 출발역 플랫폼 시각으로 되돌아가 과거 열차를 조회하던 문제 제거
- 메인 검색창 아래 `지금 / -5 / -1 / 선택시간 / +1 / +5` 빠른 시간조절 추가
- 기본값 `지금`; 사용자가 바로 조회하면 조회 버튼을 누른 실제 현재시각 기준
- 기본 추천보다 직전 열차 1대를 별도 계산하고 `앞 열차를 탄 것 같아요` 버튼 제공


V13.2.2 — 상태코드 3 / 앞 열차 / NOW 버튼 수정
- 서울시 realtimePosition의 공식 trainSttus=3 `전역출발` 지원
- 전역출발 ETA는 statnNm 현재역 출발시각이 아니라 전역 출발시각 기준
- status 3 때문에 target arr < current dep가 되어 ETA가 익일로 넘어가던 24시간 wrap 수정
- 추적 ETA 6시간 sanity guard 추가
- `앞 열차를 탄 것 같아요`는 버튼 클릭시각이 아니라 해당 앞 열차의 계산된 승차시각을 boardedAt으로 사용
- 실제 클릭시각은 trackingStartedAt 별도 저장
- NOW 버튼 초기 강조 제거
- 첫 조회는 실제 조회시각을 사용한 뒤 시간값 고정
- 사용자가 NOW를 누른 경우 다음 조회 1회에 적용하고 조회 직후 강조 해제
- 코레일 계열의 출발 상태 미반환은 별도 합성하지 않고 기존 진입/도착 기반 처리를 유지


V13.2.2 — 신규 노선 + 경인선 급행 최신 시간표
- 경강선 추가 (realtimePosition ID 1081)
- 서해선 추가 (1093)
- 경춘선 추가 (1067)
- 함께 업로드된 공항철도 시간표 추가 (1065)
- 1호선에서는 K10xx~K14xx 경인선 급행 계통만 최신 파일로 교체
- 그 외 1호선 일반/경부·장항 급행 등은 변경하지 않음
- 통과시각이 있는 급행/직통열차는 call=false로 보존
- 통과역은 위치·지연 추적에는 사용하지만 승하차 후보에서는 제외
- 신규 노선 포함 route_graph 재생성


V13.2.2 — 신규 노선 frontend line registry 수정
- 경춘선/경강선/서해선/공항철도가 backend에는 추가됐지만 frontend hidden segment select의
  lines 배열에는 빠져 있어 신규 노선이 1호선으로 fallback되던 문제 수정
- /api/stations의 STATIONS_BY_LINE 키를 frontend 지원노선 source of truth로 동기화
- lineOpts가 미등록 노선을 받아도 첫 옵션으로 fallback하지 않고 해당 값을 보존
- 사용자 보고 사례 `경춘선 망우→춘천`을 ETA 계산 단계까지 그대로 유지하도록 검증


V13.2.2 — 다중 경로 ETA 선택
- 기존: 정적 route_graph Dijkstra 최단경로 1개 선택 후 그 경로만 실시간 보정
- 변경: route graph의 상위 simple-path 후보를 최대 8개 생성
- Yen K-shortest-path 알고리즘을 내부 구현하여 외부 dependency 추가 없음
- 동일 segment 경로는 dedupe
- 1차: 실제 출발시각의 공식 시간표로 배차대기 + 환승시간을 반영해 후보 채점
- 2차: 후보 전체를 realtimePosition 기반 ETA로 재채점
- realtimePosition cache를 후보 간 공유하여 노선별 API 호출은 1회로 제한
- 최종 도착 ETA가 가장 빠른 경로 선택
- ETA 동률이면 환승 횟수가 적은 경로 우선
- UI에 비교한 후보 수와 다른 경로 최대 3개 표시
- 마포구청→춘천처럼 정적 차내시간은 비슷하지만 배차가 크게 다른 경로를 구별 가능


V13.2.2 — 빠른 환승 데이터 전체 재반영
- 서울교통공사 수도권 도시철도 환승 데이터(2025-03-17)를 현재 지원 노선 기준으로 전체 재파싱
- 경춘선/공항철도/서해선 추가 전에 빠졌던 환승 조합을 일괄 추가
- 망우 경의중앙선↔경춘선 방향별 빠른 환승 위치 반영
- 상봉/별내/공덕/홍대입구/디지털미디어시티/김포공항/서울역/초지 등 누락 조합 추가
- CSV 소요시간이 있으면 실제 값을 사용
- 소요시간이 없으면 기존 거리 기반 시간 또는 240초 fallback
- 소요시간이 없어도 빠른 하차 위치는 표시
- 신내 6호선↔경춘선은 원본 CSV에 데이터가 없어 240초 fallback 유지


V13.2.2 — 조회 멈춤/성능 핫픽스
- 다중 후보 경로 채점에서 all_trains / route_trains 결과 캐시
- 시간표 후보의 예상 소재 계산을 상위 30개 후보에만 수행
- realtimePosition API를 노선별 순차 호출하지 않고 최대 6개 병렬 조회
- 서울 API 개별 timeout을 12초에서 5초로 단축하고 실패 시 시간표 fallback
- /api/route도 사용 노선 realtimePosition을 병렬 prefetch
- FastAPI의 동기 계산을 run_in_threadpool로 이동해 event loop 장시간 점유 방지
- 프론트 fetch 20초 timeout 추가: 무한히 '조회 중' 상태로 남지 않음
- V13 다중 경로 ETA 비교 기능은 유지


V13.2.2 — 탑승 추적 회귀 수정
- V13.2 calculate_live_trip()에서 정의되지 않은 position_cache 참조로 /api/trip_update가 500을 내던 회귀 버그 수정
- 추적 요청에 필요한 노선 realtimePosition을 병렬 prefetch 후 요청 내부에서 공유
- 추적 패널 `최종 도착` 제거
- 추적 패널 3개: 현재 열차 / 현재 위치 / 다음 환승까지
- 마지막 구간에서는 `목적지 하차까지`, 환승 중에는 `환승 완료까지`로 자동 변경
- 탑승 버튼 직후 API 응답 전에도 직전 ETA 결과를 이용해 위치/남은 시간을 즉시 표시
- 추적 API 실패 시 화면값을 비우지 않고 유지한 채 다음 주기에 재시도


V13.2.2 — GA4 열차 추적 기능 사용 집계
- 실제 startTracking() 실행 시 GA4 커스텀 이벤트 `train_tracking_start` 전송
- `이 열차를 탔어요` / `앞 열차를 탄 것 같아요` 모두 같은 이벤트로 집계
- 이벤트 파라미터: line / train_no / segment_index
- 20초 자동 추적 갱신, 수동 새로고침, session restore에서는 이벤트를 다시 보내지 않음
- gtag 차단/실패가 서비스 동작을 방해하지 않도록 안전 처리
- GA4 Explore에서 Event name=train_tracking_start + Total users로 추적 기능 사용자 수 확인 가능


---

## V13.2.2 — 배포 준비 버전 (요약)

> README.txt 상단에 있던 현재 릴리스 요약입니다. 원문 그대로 옮겼습니다.

```
주요 개선
1) 출발시각
- 정확한 시각 입력
- 지금 / +5 / +10 / +15 / +20 / +30분 빠른 선택
- 기본값은 현재시각 +10분

2) 즐겨찾기
- 현재 경로와 환승시간, 운행일을 브라우저 localStorage에 저장
- 즐겨찾기 이름 지정 / 불러오기 / 삭제
- 출발시각은 매번 달라지는 값이라 저장하지 않음

3) UI
- 개발용 프리셋 제거
- 실제 서비스형 경로 빌더 / 결과 화면으로 정리
- 1~9호선 노선 색상 표시
- 모바일 반응형 개선
- 오류/로딩/신뢰도 표시 단순화

4) 배포
- 환경변수 SEOUL_API_KEY 지원
- 환경변수 PORT가 있으면 0.0.0.0:$PORT 로 실행
- 로컬에서는 기존처럼 API 키를 콘솔에서 입력
- Procfile 포함
- 외부 Python 패키지 없음

로컬 실행
1. 압축 해제
2. run.bat
3. 서울 열린데이터광장 API 키 입력
4. 브라우저 자동 실행

배포 시 필수 환경변수
SEOUL_API_KEY=<서울 열린데이터광장 인증키>

서버 시작 명령
python app.py

주의
- 현재 즐겨찾기는 브라우저 localStorage 기반이므로 다른 기기/브라우저와 동기화되지 않습니다.
- 사용자 계정 기반 즐겨찾기가 필요해지면 Supabase 등 DB를 붙이는 다음 단계가 필요합니다.
- 공휴일 자동 판정은 아직 요일 기반이므로 평일 공휴일은 '일요일·공휴일'을 직접 선택해야 합니다.
```


## V13.4.0 — 코레일 지연 캐시 + 직전열차 후보
- 코레일 계열에서 열차가 realtimePosition에 일시 미포착될 때 마지막으로 확인한 exact-train 지연을 브라우저 localStorage에 최대 35분 보존
- 20분까지 지연값 유지, 20~35분은 선형 감쇠, 새 실시간 관측이 들어오면 즉시 교체
- 캐시된 지연은 다른 열차의 중앙값 지연으로 전파하지 않고 같은 열차번호에만 적용
- Vercel 무상태 환경에서도 같은 이용자의 재조회에서 지연이 갑자기 0분으로 리셋되는 현상을 완화
- 추천 열차의 직전 열차 1대를 후보 열차 목록 맨 앞에 추가하고 `직전` 배지 표시
- 팀원이 추가한 GA4 퍼널 이벤트는 유지


## V13.4.2
- 빠른 환승 위치 재조사 및 누락 directional pair 보완
- 사용자 제공 `환승시간 추가.xlsx` 시간값 반영
- `제자리 환승`/공용승강장 0초 처리 지원
- Python의 falsy `0` 때문에 240초 fallback으로 바뀌던 transfer 로직 수정
- 팀원 GA 퍼널 이벤트 유지
