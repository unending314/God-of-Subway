# V14.9.8 — 2026-08-21

- 2호선 순환선에서 추천 방향과 반대 방향의 `직전` 열차가 표시되던 오류 수정
- 2호선 사용자 후보군을 선택된 내선/외선 방향으로 제한
- `근처 열차 후보` → `다른 열차 후보`, `앞 열차를 탄 것 같아요` → `다른 열차를 탄 것 같아요`
- 다른 열차 버튼은 즉시 탑승 처리하지 않고 후보 목록을 펼치도록 변경
- 후보 요약의 `미포착 열차 포함` 문구 제거, 개별 후보 `미포착` 배지는 유지
- 후보 출발/도착 시각 시인성 강화
- 2호선 실시간 열번 직접 매칭 실패 시 내/외선·현재역·관측시각 기반 보수적 문맥 매칭 추가

# V14.9.7 — 2026-08-21

- 대안 경로 목록 기본 접힘 및 `경로 직접 선택` 가시성 강화
- 자동경로 요약의 중복 ETA/후보 비교 문구 제거
- 현재 운행상황 요약에서 중복 도착시각/설명문 제거, 총 소요시간·도착예정 강조
- 모바일 `지금 기준으로 다시 찾기` 로딩 중 버튼 크기 고정
- 모바일 열차 추적 `현재 위치` 영역을 전폭 배치해 긴 역명 줄바꿈 개선

## V14.9.6 — 빠른환승 위치 완성 및 실험 UI 정리

- 사용자 제공값으로 빠른환승 실제 잔여 6개 방향 조합을 모두 보강했습니다.
- 상세 설정에서 `기존 앱 예상 총시간`, `기말 실험 자동 기록`, `실험 도구 화면 표시` UI를 제거하고 자동 실험 기록을 기본 비활성화했습니다.

## 빠른환승 방향 정규화
- 빠른환승 위치 매칭을 `첫 시간표 열차의 다음 정차역` 하나에 의존하던 방식에서, 실제 시간표의 운행 순서를 이용해 환승 방향키를 정규화하는 방식으로 변경했습니다.
- 급행이 인접역을 통과하는 경우에도 물리적 방향의 기존 환승 위치를 찾을 수 있습니다.
- GTX-A 서울역/수서, 김포골드라인 김포공항, 신림선 샛강처럼 환승역이 종착역인 경우에는 마지막 접근 방향키를 사용합니다.
- 6호선 응암순환처럼 일방통행 구간에서 데이터가 접근역 기준으로 기록된 경우도 안전하게 매칭합니다.
- `대모산입구`와 시간표 내부 `대모산` 표기를 동일 역으로 정규화했습니다.
- 국토교통부 원본을 다시 대조해 인천시청 인천2호선→인천1호선의 4개 방향 조합을 올바르게 복원했습니다.
- 합정→연신내→운정중앙 예시에서 연신내 6호선→GTX-A 빠른환승 `6-2`가 정상 표시됩니다.
- 현재 시간표 기준 가능한 환승 방향 조합을 재감사한 결과, 빠른환승 위치가 실제로 남아 있지 않은 조합은 6개입니다.
- 전체 회귀 테스트 65개 통과.

# V14.9.4 — 2026-08-20

## 현재시각 기준 경로 재검색
- 이동 경로 헤더의 계산 기준시각 옆에 `지금 기준으로 다시 찾기` 버튼을 추가했습니다.
- 버튼은 일반 경로 결과가 계산된 뒤에만 표시되며, 현재 표시 중인 출발역/도착역으로 현재시각 기준 자동경로 후보 비교를 다시 수행합니다.
- 상단 입력부까지 스크롤하지 않아도 동일한 이동의 최신 최적경로를 다시 탐색합니다.
- `이 열차를 탔어요`로 추적을 시작하는 즉시 버튼을 숨기고, 추적 중에는 기존 `↻ 새로고침`만 유지합니다.
- 세션에서 추적 상태를 복원한 경우에도 재검색 버튼을 노출하지 않습니다.


## V14.9.3 — 모바일 시간 선택 / 검색 아이콘 렌더링 수정
- 모바일 직접 시간 선택을 OS 원형 시계 피커 대신 앱 내부 3열(오전·오후 / 시 / 분) 피커로 교체.
- 경로 조회 후 버튼에 남던 SVG 기본 fill 삼각형을 제거하고 `경로 조회` 텍스트 상태로 복원.
- 역 자동완성 노선 아이콘을 inline SVG 대신 브라우저 호환성이 높은 HTML/CSS 배지로 렌더링.
# V14.9.3 — 2026-08-20

- `God-of-Subway-dev-ts-bun`의 초기 검색 입력 UI만 이식했습니다.
- 출발/도착역 카드, 원형 역 전환, 직접 시간 선택, ±1/±5분, 경로 조회 UI를 동일 계열 디자인으로 정리했습니다.
- V14.8의 결과 화면, 대안경로 선택, 1호선 추월 보정 로직은 유지합니다.
- 상세 설정은 기능 보존을 위해 검색 카드 하단 보조 버튼으로 유지합니다.

# V14.8.0 — 2026-08-20

- 1호선 공식 시간표에서 완행 대피정차 중 급행이 통과해 운행순서가 뒤집히는 계획 추월 관계를 자동 검출.
  - 평일 59건, 휴일 48건.
  - 평일 추월역: 군포 38, 오산 8, 도봉산 4, 수원 4, 병점 3, 평택 2.
  - 휴일 추월역: 군포 31, 오산 8, 수원 6, 병점 3.
- 실시간/최근 열차별 지연을 단순 독립 가산하지 않고, 계획 추월역 이후에는 완행이 급행보다 먼저 나가지 않도록 대피 대기시간을 ETA에 추가.
- K693(+1분) / K1951(+5분) 예시에서 신도림→성균관대 추천이 K693으로 뒤집히던 문제 수정: K1951 21:15, K693 21:16으로 계산.
- 화면에 `급행 추월 예정` / `급행 추월 대기` 운행정보를 열차 후보와 선택된 구간에 표시. 현재 실시간 지연(+N분)은 그대로 두고 미래 운전정리 대기분을 별도 표시.
- 급행 지연이 너무 커 추가 대피가 10분을 넘는 경우에는 강제 순서보정 대신 `운전정리 변동 가능`으로 표시.
- 추월 관계는 `route_graph.json`에 사전 계산해 저장하여 런타임 전수검색 없이 즉시 조회.
- 추월 이전 목적지에는 대피 보정을 적용하지 않는 회귀 테스트 추가. 전체 55 tests passed.

# V14.7.0 — 2026-08-20

- 자동경로 응답의 추천안/대안안에 사용자 선택 버튼 추가. 선택 시 해당 segments를 즉시 로드하고 ETA를 재계산.
- 우측 상단 버전 표기를 `/api/health`의 서버 버전과 단일화하여 하드코딩 불일치 제거.
- 4호선↔수인분당선, 서해선↔경의중앙선 공유선로에서 동일 진행방향 환승 보행시간을 0초로 보정. 열차 대기시간은 기존 시간표 계산에서 별도로 유지.
- 금정 1호선↔4호선 64초 짧은 환승시간 유지 및 회귀 테스트 추가.

# V14.6.0 — 2026-08-20

- 총신대입구(이수) 4호선↔7호선 환승시간을 사용자 제공 8개 방향 조합으로 반영.
- 4호선→7호선: 동작→남성 190초, 동작→내방 230초, 사당→남성 190초, 사당→내방 230초.
- 7호선→4호선: 남성→동작 230초, 남성→사당 230초, 내방→사당 190초, 내방→동작 190초.
- 자동 경로 탐색의 방향 미확정 단계에서는 보수적으로 230초를 대표 환승비용으로 사용하며, 실제 경로 확정 후 정확한 방향별 시간으로 재계산.
- route_graph 재생성 및 방향별 환승 회귀 테스트 추가.

# V14.5.0 — 2026-08-20

- 사용자 완성 환승 CSV 반영.
  - 빠른환승 위치 입력 대상 132개 방향 조합을 모두 반영.
  - 환승시간 입력 대상 103개 방향을 모두 반영.
  - 신규 34개 환승 관계는 양방향 모두 빠른환승 위치가 존재하도록 완료.
  - `인천시청 인천1호선 → 인천2호선`은 사용자 메모에 따라 기본 260초, 인천1호선 하행(예술회관 방향)에서만 130초 적용.
- 기존 국토부 공개데이터의 방향 엄격 매칭 정책 유지.
- `transfer_data.json` 메타데이터에서 삭제된 과거 master/version 흔적을 정리하고 런타임 JSON을 minify.
- 회귀 테스트 기대값을 새 환승시간에 맞게 갱신.

# V14.4.0 — 2026-08-20

- 국토교통부 `철도역 빠른 환승 정보_20250923` 반영.
  - 기존 위치 데이터는 보존하고, 비어 있던 환승 관계에 한해서 방향이 확정되는 차량-문 위치만 추가.
  - 추가 8개 노선 관련 13개 환승 관계에서 최소 한 방향 이상 보강.
  - 신림선/GTX-A/김포골드라인은 해당 공개데이터에 없어 이번 패치 대상에서 제외.
  - 공개데이터에 환승시간 정보가 없어 신규 관계는 180초 fallback 유지.
- `direction_strict` 레코드 지원. 공개데이터에 명시된 방향 조합이 아니면 빠른환승 위치를 다른 방향에 추정 적용하지 않음.
- 기존 V14.3 clean 프로젝트 구조 유지.

# V14.3.0 — 2026-08-20

- 인천2호선 서해구청 실제 역 시각표 반영.
  - 평일 465개 운행편, 주말/휴일 369개 운행편 모두 기존 시간표 운행편과 매칭.
  - 기존 보간값 833개를 실제 도착/출발 시각으로 교체.
  - 서해구청 관련 `estimated=true` 제거.
  - 인천2호선 전체에서 남은 보간 stop은 95개이며 서해구청 외 원본 공란에 한정.
- 프로젝트 clean-up.
  - 중복 `base/` 제거.
  - V13/V14 과거 버전별 문서 및 audit 산출물 제거.
  - 인천1/2호선 import 원본/중간 CSV/XLSX/JSON 제거.
  - 실행에 사용하지 않는 transfer master 원본과 변환 도구 제거.
  - 런타임 JSON minify 적용.
- 기존 realtime foundation, 8개 추가노선, GTX-A 단절 구조 유지.
