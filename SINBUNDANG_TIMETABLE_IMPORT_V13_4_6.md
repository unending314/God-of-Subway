# V13.4.6 신분당선 시간표 변환·검증

> **V13.4.7 변경:** 신분당선은 더 이상 schedule-only 노선이 아닙니다. 서울시 realtimePosition의 `subwayId=1077` 실시간 위치를 사용하며, 이 문서의 schedule-only 설명은 V13.4.6 당시 상태를 기록한 것입니다.

## 원본
- 파일: `신분당선 시간표.xlsx`
- SHA-256: `759e2b3ead7441f7aae38436e473b8194d7a91e642c40ee35198c74ce30d8bc6`
- 사용자가 Rail.Blue 노선 시각표를 좌우로 넘기며 직접 복사한 자료
- 기준일: 2026-08-19 평일, 2026-08-22 토요일, 2026-08-23 일요일

## 시트와 편수
| 시트 | 고유 열차 |
|---|---:|
| 평일 상행 | 163 |
| 평일 하행 | 163 |
| 주말 상행 | 136 |
| 주말 하행 | 136 |

평일 총 326편, 주말 총 272편이다. 화면을 옆으로 넘길 때 앞 열차가 다음 화면 첫 열에 반복되는 구조가 있어 반복 열은 열차번호 기준으로 제거했다. 반복 열에서 발견된 차이는 `─` 같은 화면 경계 표식뿐이며 열차 시각 데이터는 동일했다.

## 변환 규칙
- Excel 시간값은 초 단위 정수로 변환한다.
- 자정 이후 운행이 앞 시각보다 작아지는 경우 서비스데이 기준으로 86400초를 더해 24:xx 형식으로 보존한다.
- 여객역 16개: 신사, 논현, 신논현, 강남, 양재, 양재시민의숲, 청계산입구, 판교, 정자, 미금, 동천, 수지구청, 성복, 상현, 광교중앙, 광교.
- `광교기지`, `분당연결선분기`, `판교주박기지`는 운전상 지점으로 `call=false` 처리한다.
- Rail.Blue의 `↓`는 운전상 통과 표식으로 보고 여객 정차로 취급하지 않는다.
- 주말 하행 일부 열차는 상세 행이 광교에서 끝나지만 상단 종착 정보가 `광교기지`로 표시된다. 이 경우 상단 종착 시각을 이용해 비여객 종착점을 보완한다.
- 업로드 파일에는 토/일 별도 시트가 없고 하나의 `주말` 시각표만 있으므로 SAT와 END가 동일 데이터를 사용한다.

## 앱 동작
- 신분당선은 `SCHEDULE_ONLY_LINES`에 포함된다.
- 서울시 realtimePosition API를 호출하지 않는다.
- ETA, 직전 열차, 자동 환승 경로는 `sinbundang_schedule.json`을 사용한다.
- 실시간 지연을 알 수 없으므로 기본 신뢰도는 `낮음`이다.
- 자동 환승은 기존과 동일하게 같은 역명을 공유하는 노선 사이에 환승 간선을 만들며, 별도 환승시간 데이터가 없으면 기본 240초를 사용한다.

## 검증
- 원본 변환 중 시간 역전/시발·종착 누락/호출 가능 운전상 지점: 0건
- `engine.timetable_integrity_report()`: issue_count 0
- 신분당선 평일/주말 편수 검증
- 평일 첫 하행 DX9003: 신사 05:30 출발 검증
- 주말 SAT/END 동일 프로필 검증
- 신분당선 계산 시 realtime API 미호출 검증
- route_graph 신분당선 간선 및 `광교→역삼` 신분당선→2호선 자동 환승 검증
- 전체 회귀 테스트 21/21 통과
