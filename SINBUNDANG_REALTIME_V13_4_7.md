# V13.4.8 신분당선 실시간 위치 연동

## API 식별

서울시 `realtimePosition` 공식 명세에서 신분당선의 응답 `subwayId`는 `1077`이다.
요청 URL의 필수 노선 인자는 `subwayNm`이므로 실제 요청은 `신분당선`을 사용하고, 응답 행에 `subwayId`가 존재하면 `1077`과 일치하는 행만 사용한다.

## 열차번호 매칭

앱 시간표는 Rail.Blue 기반 `DX####` 열번을 사용한다. 서울시 응답 열번에 접두사가 없거나 표기 차이가 있을 수 있으므로 다음 순서로 매칭한다.

1. 전체 열번 exact match
2. 숫자부 match (`DX9003` ↔ `9003`)
3. 숫자부가 하루 시간표에서 유일할 때만 채택

현재 신분당선 평일/주말 시간표에서 숫자부 충돌은 0건이다.

## 신뢰도

- 동일 열차번호 실시간 관측: `live_exact` / 높음
- 동일 방향·등급 다른 열차 지연 중앙값: `live_median` / 중간
- 최근 exact 열차 캐시: `cached_exact` / 중간
- API 실패 또는 시간표와 전혀 매칭되지 않음: `schedule_only` / 낮음

## 장애 안전장치

- API 요청 실패는 전체 경로 계산 실패로 전파하지 않고 공식 시간표 ETA로 자동 강등한다.
- 응답의 `subwayId`가 다른 노선이면 해당 행을 무시한다.
- 신분당선도 최근 exact-train 지연 캐시 대상에 포함한다.

## 회귀 테스트

- `LINE_IDS["신분당선"] == "1077"`
- prefetch가 신분당선 realtime API를 실제 호출하는지 검증
- `9003` 실시간 열번이 `DX9003`에 매칭되는지 검증
- exact 실시간 관측 시 신뢰도 `높음`으로 승격되는지 검증
- 잘못된 `subwayId` 응답이 필터링되는지 검증
