# 신분당선 V13.5.0 설계

## 전제

신분당선은 공개 운행열차번호를 서비스의 식별자로 사용하지 않는다. 과거 수집본에 포함됐던 `DXxxxx` 값은 실제 공개 열차번호로 신뢰할 수 없으므로 생산 데이터와 ETA 로직에서 제거한다.

서울시 `realtimePosition` 응답의 `trainNo`는 신분당선에서 **현재 운행 차량을 구분하는 vehicle_id**로 취급한다. 이 값은 공개 시각표 열번과 매칭하지 않는다.

## 데이터 역할

- `sinbundang_public_timetable.json`: 평일/주말 **역별 출발시각**. 열차번호 또는 열차 간 연결 관계를 만들지 않는다.
- `sinbundang_runtime.json`: 신분당선 운영사 공개 **역간 소요시간**. 신사→광교 42분 02초, 광교→신사 41분 54초가 되도록 역간 주행시간과 중간 정차 30초를 합산한다.
- `realtimePosition`: 차량번호(vehicle_id), 현재역, 방향, 종착역, 차량상태, 수신시각을 이용한다.

## ETA

실시간 차량이 승차역 이전에 있으면 차량 상태를 기준으로 현재 위치의 출발 시각을 추정한다.

- 진입: 현재역 출발까지 약 45초
- 도착: 현재역 출발까지 약 25초
- 출발: 수신시각을 현재역 출발 기준으로 사용
- 전역출발: 현재 표시역의 직전 역 출발을 수신시각 기준으로 사용

그 뒤 운영사 역간 소요시간을 누적해 승차역 출발 ETA를 만들고, 승차역→하차역 공식 소요시간을 더해 하차 ETA를 계산한다.

**지연시간 자체는 계산하지 않는다.** 앱에는 `지연 미산정`으로 표시한다.

## 차량 미포착 fallback

승차 가능한 실시간 차량을 찾지 못하면 공개 역별 시각표에서 다음 출발시각을 찾는다. 이 후보는 `시간표 예정`, 신뢰도 `낮음`이며 차량 식별값이 없기 때문에 `이 열차를 탔어요` 추적 대상으로 사용하지 않는다.

## 탑승 추적

실시간 후보의 `차량 N`을 사용자가 탑승했다고 선택하면 동일 API vehicle_id만 고정 추적한다. 차량이 순간적으로 API에서 사라지는 경우 탑승시각 + 운영사 공식 소요시간으로 임시 ETA를 유지하고, 다시 포착되면 같은 차량의 소재로 갱신한다.

## 폐기한 로직

- `DXxxxx ↔ realtimePosition.trainNo` 매칭
- 숫자부 fallback을 통한 신분당선 열번 매칭
- `realtimeStationArrival.btrainNo`와 DX 열번 매칭
- 신분당선 열차별 지연시간 추론
- DX 열번을 사용자 화면에 표시
