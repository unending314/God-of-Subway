# V13.4.10 빠른 환승 위치 마스터

## 목적

신분당선 추가 때마다 환승 위치를 다시 조사하는 문제를 없애기 위해, 현재 지금타 지원 노선과 향후 추가 가능성이 있는 수도권 전철 노선을 하나의 방향별 마스터로 관리한다.

빠른 환승 위치는 **역/출발노선/출발방향/도착노선/도착방향**을 모두 구분한다. 종착역에서 실제로 출발할 수 없는 방향은 생성하지 않는다. 방향 키는 가능한 경우 환승역을 지난 뒤의 다음 여객역명을 사용한다.

## 소스와 우선순위

1. `user_direct_verification` — 사용자가 원문을 직접 확인해 정정한 값
2. `newer_namu_station` — 더 최근의 개별 역 문서
3. `namu_line_transfer_page` — 사용자가 지정한 노선별 수도권 전철 환승 정보 문서
4. `official_public_data` — 국토교통부/국가철도공단 공개 데이터
5. `legacy_app` — V13.4.9까지 검증해 운용하던 기존 위치값

사용자가 제공한 원본 NamuWiki URL은 `transfer_position_master.json`의 `canonical_namu_sources`에 보존했다. 이 실행환경에서는 `namu.wiki` 직접 접근이 차단되는 경우가 있어 최근 미러/검색 인덱스로 본문을 확인했고, 가능한 항목은 공공데이터와 교차검증했다.

## 산출물

- `transfer_data.json`: 현재 앱에서 즉시 사용하는 DB
- `transfer_position_master.json`: 향후 노선까지 포함한 전체 마스터
- `transfer_position_master.csv`: 사람이 검토/수정하기 위한 평면 CSV
- `TRANSFER_POSITION_CONFLICTS.json`: 소스끼리 값이 충돌한 사례와 채택 근거
- `transfer_position_needs_verification.csv`: 아직 숫자를 확정하지 못한 방향 조합 146개

### 현재 통계

- 앱용 `transfer_data.json`: **294 pairs / 942 directional records**
- 신분당선 신규: **16 directional line-pairs / 64 direction-combination records**
- 전체 마스터: **1,138 records**
  - 현재 앱: 942
  - 향후 네트워크 scaffold: 196
  - 위치 확인 완료: 992
  - 추가 확인 필요: 146

`needs_verification`은 값이 없는 것이 확인된 방향 조합이다. 반대 방향 값을 대칭으로 복사하거나 첫 번째 후보를 임의 채택하지 않는다.

## 중요 충돌

### 신논현

- 대상: `신분당선 광교 방면 → 9호선`
- 사용자 원문 확인: **6-3**
- 일부 미러에서 관측된 값: 6-4
- **채택: 6-3** (`user_direct_verification` 최우선)

현재 앱에서 신분당선 `논현→신논현`으로 도착하면 방향 키가 `강남`으로 계산되며 9호선 일반/급행 어느 방향으로 환승하든 하차 빠른 위치 `6-3`을 선택한다.

### 양재

- 대상: `3호선 대화 방면 → 신분당선`
- 최신 개별 역 문서: 5-4
- 이전 노선별 환승 문서: 5-3
- **채택: 5-4** (더 최근 개별 역 문서 우선)

## 신분당선 즉시 반영 범위

신사(3), 논현(7), 신논현(9), 강남(2), 양재(3), 정자(수인분당), 미금(수인분당), 판교(경강)의 양방향 pair를 `transfer_data.json`에 추가했다.

문서/공공데이터에서 방향을 확실히 구별할 수 있는 위치는 즉시 채웠다. 정자·미금처럼 공개 데이터에 후보 위치는 있으나 **출발 방향을 확정할 수 없는 레코드**는 `needs_verification`으로 두어 잘못된 빠른 환승 위치가 화면에 표시되지 않게 했다.

## 엔진 보정

신분당선 DIA에는 `판교주박기지` 같은 `call=false` 운전상 지점이 있다. 기존 방향 힌트 함수가 이를 다음 역으로 반환하면 판교의 `3-1/4-3` 방향 레코드가 정확히 매칭되지 않을 수 있었다.

V13.4.10에서는 `_direction_station()`과 `_outgoing_direction_station()`이 `call=false` 지점을 건너뛰고 **다음 여객 정차역**을 방향 키로 사용한다.

예:

- 청계산입구 → 판교 → (판교주박기지) → 정자
- 판교 도착 방향 키 = `정자`
- 신분당선 광교 방면 → 경강선 = `3-1`

## 환승시간과의 관계

이 마스터는 **빠른 환승 위치**를 관리한다. 환승 소요시간은 별도 필드이며 아직 정확 시간이 없는 pair에서는 기존 `default_seconds=240` fallback이 유지된다. 위치값 확인이 환승시간 4분을 의미하지 않는다.

## 향후 노선 추가 절차

1. `transfer_position_master.json`에서 해당 노선/환승역 방향 레코드를 조회한다.
2. `status=verified*`이면 앱 `transfer_data.json`로 승격한다.
3. `needs_verification`이면 원문/공공데이터를 재확인한 후 채운다.
4. source와 날짜/충돌 이력을 보존한다.
5. 방향 조합 회귀 테스트 후 배포한다.
